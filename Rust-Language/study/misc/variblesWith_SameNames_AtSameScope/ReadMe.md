# Rust Allow Multi-Declaration of varibles with **==Same Name==**  at the **==Same scope==** 

```rust
fn main()
{
    let a: i32 = 10;
    let b: i32 = 20;  // the Rust-Language Compile will **==Pass==** the 2nd statement rather than treating it as an **==error==**  
}
```


